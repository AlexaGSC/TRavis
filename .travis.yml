language: node:js
nodejs:
  - stable
branches:
  only:
  - build_gh
after_success:
  - bash scripts/deploy.sh
deploy:
  local_dir: dist
  provider: pages
  skip_cleanup: true
  github_token: $github_token
  edge: true
  keep_history: true
  on:
    branch: build_gh